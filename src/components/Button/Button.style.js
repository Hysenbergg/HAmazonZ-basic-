import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        borderRadius: 10,
        backgroundColor: '#2286c3',
        padding: 8,
        margin: 10,
        alignItems: "center",
    },
    title: {
        fontWeight: "bold",
        fontSize: 18,
        color: "white"
    }
})